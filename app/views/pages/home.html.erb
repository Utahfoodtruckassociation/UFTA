<div class="home-container" id="startchange">
	<div class="row">
		<div class="col-md-9">
			<div id="tabs">
			  <ul>
			    <li><a href="#fragment-1"><span class="btn">Map</span></a></li>
			    <li><a href="#fragment-2"><span class="btn">Calendar</span></a></li>
			  </ul>
			  <div id="fragment-1">
			    <div style='width: 100%;'>
					  <div id="map" style='width: 100%; height: 600px;'></div>
					</div>
			  </div>
			  <div id="fragment-2">
			  	<iframe src="https://calendar.google.com/calendar/embed?src=thiepseats.com_5tn760321cf7joc58io237hfa4%40group.calendar.google.com&ctz=America/Denver" style="border: 0" width="100%" height="600" frameborder="0" scrolling="no"></iframe>
			  </div>
			</div>
		 
				<script>
				$( "#tabs" ).tabs();
				</script>
		</div>
		<!-- <div class="col-md-3 center-sidebar"> -->
		<div class="col-md-3 truck-list">
			<%= render 'shared/home_sidbar' %>
		</div>
	</div>		 
</div>

<% @calendars.items.each do |c| %>
	<%= c.summary %>
<% end %>

<script type="text/javascript">
  handler = Gmaps.build('Google');
handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
  markers = handler.addMarkers(<%=raw @hash.to_json %>);
  handler.bounds.extendWith(markers);
  handler.fitMapToBounds();
  if (markers.length == 1) {
  	handler.getMap().setZoom(17);
  };
  // handler.centerOn(markers);
});
</script>
